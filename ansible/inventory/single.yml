all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    github_deploy_key_path: /root/.ssh/id_ed25519
    compose_file: docker/docker-compose.prod.yml
  hosts:
    contabo:
      ansible_host: "{{ lookup('env', 'CONTABO_IP') }}"
