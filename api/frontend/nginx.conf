server {
    listen 5173;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /logs/ {
        try_files $uri $uri/ /logs/index.html;
    }

    location = /logs {
        return 301 /logs/;
    }

    location / {
        return 301 /logs/;
    }
}
